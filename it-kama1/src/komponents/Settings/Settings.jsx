import React from "react";
import s from './Settings.module.css';

const Settings = (props) => {
    return (
        <section>
            settings
        </section>
    )
}
export default Settings;